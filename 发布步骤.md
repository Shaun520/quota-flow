# 发布步骤

> 记录日期：2026-08-13

## 当前发布版本

- 发布版本：`0.1.1`
- 仓库：`Shaun520/quota-flow`
- GitHub Releases：https://github.com/Shaun520/quota-flow/releases

## 发布前置条件

- Windows 环境
- 已安装 `pnpm`
- 已配置 GitHub Token：`GH_TOKEN`
- Token 权限至少包含 `public_repo`

## 发布流程

1. 修改版本号。

   文件：`apps/desktop/package.json`

   ```json
   {
     "name": "@quota-flow/desktop",
     "version": "0.1.1"
   }
   ```

2. 本地验证。

   ```powershell
   pnpm --filter @quota-flow/desktop typecheck
   pnpm --filter @quota-flow/desktop build
   pnpm --filter @quota-flow/desktop release:dir
   ```

3. 设置 GitHub Token。

   cmd：

   ```cmd
   set GH_TOKEN=你的token
   ```

   PowerShell：

   ```powershell
   $env:GH_TOKEN="你的token"
   ```

4. 发布到 GitHub Releases。

   ```powershell
   pnpm release:publish
   ```

   脚本会自动先拉取远端 tag：

   ```text
   git fetch --tags origin
   ```

   这一步很重要。`electron-builder` 创建正式 release 时要求 GitHub 上有有效 tag，如果本地没有 tag，会报 `422 Published releases must have a valid tag`。

   实际执行：

   ```text
   git fetch --tags origin
   pnpm --filter @quota-flow/desktop run release:publish
   pnpm run build
   electron-builder --win --publish always
   ```

   发布类型已配置为 `releaseType: release`，不会再默认生成 draft。

## 发布结果确认

发布成功后，到 GitHub Releases 页面查看，确认不是 `Draft`：

https://github.com/Shaun520/quota-flow/releases

Release 资产应包含：

- `Quota-Flow Setup 0.1.1.exe`
- `Quota-Flow Setup 0.1.1.exe.blockmap`
- `Quota-Flow Portable 0.1.1.exe`
- `latest.yml`

`latest.yml` 是旧版本桌面端 `electron-updater` 判断新版本的关键文件。

如果之前已经生成了 draft，可以在 GitHub Releases 页面打开该 draft，点击 `Publish release` 把它发布成正式 release。这样 `electron-updater` 才能读取到更新。

发布后可以用下面地址验证：
```text
https://github.com/Shaun520/quota-flow/releases/latest/download/latest.yml
```

## 客户端升级路径

- 旧版本客户端启动 3 秒后自动检查更新。
- 之后每 4 小时后台检查一次。
- 用户也可以在“设置 -> 检查更新”手动检查。
- 发现新版本后提示用户下载。
- 下载完成后提示用户重启安装。

## 常见问题

### 报错：GitHub Personal Access Token is not set

原因：运行发布命令时没有设置 `GH_TOKEN`。

解决：

cmd：

```cmd
set GH_TOKEN=你的token
pnpm release:publish
```

PowerShell：

```powershell
$env:GH_TOKEN="你的token"
pnpm release:publish
```

注意：不要复制终端提示符，只输入命令内容。

### 报错：422 Published releases must have a valid tag

原因：本地缺少远程 tag，或当前版本号对应的 tag 没有推到 GitHub。

解决：先确认 tag 已同步，再重新发布。

```powershell
git fetch --tags origin
git tag --list
git rev-parse v0.1.1
$env:GH_TOKEN="你的token"
pnpm release:publish
```

如果 GitHub 上已经有一个 `Draft`，可以先在 Releases 页面打开该草案，点击 `Publish release` 发布；如果继续用脚本发布且同名 tag 已存在，通常会让 `electron-builder` 更新该 release 而不是新建草案。

### 不想发布，只本地打包

```powershell
pnpm --filter @quota-flow/desktop release
```

产物在 `apps/desktop/release/`，但本地打包不会触发旧版本自动更新。
